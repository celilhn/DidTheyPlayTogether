

namespace Domain.Constants
{
    public class Constants
    {
        public enum SourceTypes
        {
            Wikipedia = 0,
            MovieDb =1
        }

        public enum MediaTypes
        {
            movie = 0,
            tv = 1
        }
    }
}
